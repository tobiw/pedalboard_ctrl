Requirements:
- midisend program (written in rust, to be added to repo)
- http server for input via ipc (written in rust, to be added to repo)
- tkinter is built-in, or required with apt install python3-tk
- if pip install of requirements fails: pip install wheel
